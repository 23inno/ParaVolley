@model Event

<div class="modal-header">
    <h5 class="modal-title fw-bold">Event Details</h5>
    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
</div>
<div class="modal-body">
    <h4 class="fw-bold mb-3">@Model.Title</h4>
    <p class="text-muted">@Model.Description</p>
    <dl class="row mb-0">
        <dt class="col-sm-4 text-muted">Type</dt>
        <dd class="col-sm-8">@Model.Type</dd>

        <dt class="col-sm-4 text-muted">Status</dt>
        <dd class="col-sm-8">@Model.Status</dd>

        <dt class="col-sm-4 text-muted">Date &amp; Time</dt>
        <dd class="col-sm-8">@Model.Date.ToString("MMM d, yyyy") &middot; @Model.Time</dd>

        <dt class="col-sm-4 text-muted">Location</dt>
        <dd class="col-sm-8">@Model.Location</dd>

        <dt class="col-sm-4 text-muted">Participants</dt>
        <dd class="col-sm-8">@Model.Participants</dd>
    </dl>
</div>
<div class="modal-footer">
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-pv js-modal-trigger"><i class="bi bi-pencil"></i> Edit</a>
    <a asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-outline-danger js-modal-trigger"><i class="bi bi-trash"></i> Delete</a>
    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Close</button>
</div>
